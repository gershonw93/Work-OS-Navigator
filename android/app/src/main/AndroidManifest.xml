<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            How a push notification looks when the app did not draw it itself
            (the app is in the background or closed, which is nearly always).
            The icon must be a white silhouette - see ic_stat_sytenav.xml - and
            the channel is the one lib/use-push.ts creates on start, so a push
            that arrives before the app has ever opened still has somewhere
            sensible to go. ANDROID_CHANNEL_ID in lib/fcm-channel.ts is the
            same string.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_sytenav" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="sytenav" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        The same capabilities the iOS Info.plist strings cover, declared for
        Android. Without these the OS refuses the request outright and the
        feature looks broken rather than denied.

        Camera and photos: jobsite photos on daily logs, and scanning invoices,
        quotes and permits. Location: the time clock records where somebody
        clocked in and out, and where a daily log was written.

        NO PHOTO-LIBRARY PERMISSION, ON PURPOSE. Every photo SyteNav takes in
        goes through an <input type="file"> in the webview, which opens the
        SYSTEM picker - the user hands over the one file they chose, and that
        needs no permission at all. READ_MEDIA_IMAGES used to be declared here
        and Google Play refused the release over it: that permission is only
        for apps whose core purpose is the photo library (a gallery, an
        editor), and anything else must use the picker. It is REMOVED rather
        than merely left out, with tools:node="remove", so that a plugin whose
        own manifest declares it cannot merge it back in.

        All of them are optional to the app: `required="false"` on the camera
        feature keeps SyteNav installable on a tablet without one, and every
        screen works if the permission is refused.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
</manifest>
